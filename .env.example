# .env example for local deployment
# Path to your GGML model file (do NOT commit model weights to repository)
LLM_MODEL_PATH=backend/models/ggml-model.bin
# Optional: audio temporary folder
AUDIO_TEMP_DIR=/tmp/vizo_audio
